export interface Role {
  roleId: number;
  roleTypeId: number;
  roleName: string;
  roleDescription: string;
}

export interface RoleType {
  roleTypeId: number;
  roleTypeName: string;
  roleTypeDescription: string;
}
